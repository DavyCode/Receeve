{
  "name": "receeve-solution",
  "version": "1.0.0",
  "description": "receeve lambda challenge",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp package.json dist && cd dist && npm install",
    "test": "jest"
  },
  "keywords": [
    "aws",
    "lambda",
    "sns",
    "mailgun"
  ],
  "author": "davycode",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.5",
    "@types/aws-lambda": "^8.10.78",
    "aws-lambda": "^1.0.6"
  },
  "dependencies": {
    "aws-sdk": "^2.943.0",
    "dotenv": "^10.0.0"
  }
}
